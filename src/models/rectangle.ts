export default class rectangle {
    id: number;
    width: string;
    height: string;
    constructor(id: number, width: string, height: string) {
        this.id = id;
        this.width = width;
        this.height = height;
    }
}
